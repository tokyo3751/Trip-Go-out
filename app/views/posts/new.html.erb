<h1>新規投稿</h1>
  <%= form_with model: @post do |f| %>
    <h4>画像</h4>
      <%= f.file_field :image, accept: "image/*" %>
    <h4>場所名</h4>
      <%= f.text_field :place %>
    <h4>コメント</h4>
      <%= f.text_area :comment %>
    <%= f.submit '投稿' %>
  <% end %>